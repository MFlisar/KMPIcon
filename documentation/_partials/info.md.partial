This library allows you to generator icons for KMP projects.

It currently supports following:

* base on a source image:
    * generate a common icon for compose resources
    * generate a ico file for windows

the source can be the playstore icon generated by Android Studio e.g.